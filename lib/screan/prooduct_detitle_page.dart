import 'package:flutter/material.dart';

class ProductDetitlePage extends StatelessWidget {
  const ProductDetitlePage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'title',
        ),
      ),
    );
  }
}
